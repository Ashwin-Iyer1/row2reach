{
  "name": "Row_2_Reach",
  "version": "1.0.0",
  "main": "app.js",
  "build": {
    "extraResources": [
      "./extraResources/config.json"
    ],
    "extraFiles": [
      "extraResources"
    ]
  },
  "scripts": {
    "start": "electron app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildMac": "electron-packager . --overwrite --platform=darwin --arch=arm64 --out=release-builds --extra-resource=data/config.json",
    "buildWin": "electron-packager . electron-tutorial-app --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Electron Tutorial App\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^38.1.2",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "@types/papaparse": "^5.3.16",
    "dotenv": "^17.2.2",
    "electron-json-storage": "^4.6.0",
    "papaparse": "^5.5.3"
  }
}
